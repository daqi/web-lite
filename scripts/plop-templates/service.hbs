import { {{pascalCase name}}Repository } from './{{camelCase name}}.repository';
import type { Create{{pascalCase name}}Input, Update{{pascalCase name}}Input } from '../../validators/{{camelCase name}}.validator';

export class {{pascalCase name}}Service {
  private repository = new {{pascalCase name}}Repository();

  async list() {
    return await this.repository.findAll();
  }

  async get(id: number) {
    const item = await this.repository.findById(id);
    if (!item) throw new Error('{{pascalCase name}} not found');
    return item;
  }

  async create(data: Create{{pascalCase name}}Input) {
    return await this.repository.create(data);
  }

  async update(id: number, data: Update{{pascalCase name}}Input) {
    const item = await this.repository.update(id, data);
    if (!item) throw new Error('{{pascalCase name}} not found');
    return item;
  }

  async delete(id: number) {
    await this.repository.delete(id);
  }
}
